{
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare pnpm@9.15.9 --activate && pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm run vercel:preflight && pnpm run build",
  "devCommand": "pnpm run dev",
  "outputDirectory": ".next"
}
